# 视频语义搜索系统技术方案

> 目标：根据关键词搜索目标视频，并支持**画面级内容匹配**（语义模糊匹配）。
> 技术栈：Python 为主；数据源为在线视频平台 API。

---

## 1. 需求定义

### 1.1 核心能力
- **关键词搜索**：输入文本（如"海边"、"会议室 白板"、"猫 打盹"），返回相关视频。
- **画面级内容匹配**：不只是匹配标题/标签，而是匹配**画面里实际出现的内容**。即使标题没提到，只要画面里有对应内容，也能被召回。
- **语义模糊匹配**：支持同义词与模糊表达（如"海滩"能搜出"海边"），不做死板字面匹配。

### 1.2 判定指标
| 指标 | 说明 | 目标 |
|------|------|------|
| 召回率 Recall | 相关视频被搜出的比例 | ≥ 80% |
| 精确率 Precision | 搜出的视频中相关比例 | ≥ 85% |
| 检索延迟 | 单次查询耗时 | ≤ 500ms |
| 入库吞吐 | 每秒处理的视频 | ≥ 5 部/秒 |

---

## 2. 系统架构

```
┌─────────────────────── 离线索引流水线（建库） ───────────────────────┐
│ 视频采集 → 抽帧取样 → 视觉理解 → 多模态向量化 → 向量入库               │
│  (yt-dlp)  (FFmpeg)   (VLM)     (CLIP/SigLIP)  (Milvus)              │
└────────────────────────────────────────────────────────────────────────┘

┌─────────────────────── 在线检索流水线（查询） ───────────────────────┐
│ 用户查询 → 查询向量化 → 向量相似检索 → Rerank 重排 → 结果输出          │
│          (CLIP文本编码)  (Milvus ANN)    (cross-encoder)             │
└────────────────────────────────────────────────────────────────────────┘
```

两条流水线通过共享的**向量库**和**元数据库**解耦，可独立扩缩容。

---

## 3. 模块详细设计

### 3.1 视频采集层（数据源：在线平台 API）

**职责**：拉取视频元数据 + 视频流，供后续抽帧。

| 组件 | 说明 |
|------|------|
| `yt-dlp` | 支持 YouTube、Bilibili、Vimeo 及数百个平台的视频下载/元数据提取 |
| `requests` | 调用平台官方 API（如 YouTube Data API v3），获取标题、描述、标签、封面、时长 |
| `Celery + Redis` | 异步任务队列，把采集任务分发到多 worker 并行处理 |
| `FFmpeg` | 拉流、转码、抽帧 |

**流程**：
1. 通过平台 API 按关键词/频道/收藏分页拉取视频列表（含 `video_id`、`title`、`description`、`tags`、`duration`）。
2. 元数据写入 PostgreSQL。
3. 用 `yt-dlp` 拉取视频文件（或直接拉流），交给抽帧层。
4. 失败任务自动重试 3 次，超时进入死信队列。

**关键设计**：
- 对 `video_id` 做**去重**（幂等），避免重复入库。
- 采集节流，遵守平台 API 的速率限制和 Robots 协议。

### 3.2 抽帧取样层

**职责**：把长视频压缩为少量有代表性的关键帧，控制后续计算成本。

**策略**（两种结合）：
1. **时间均匀采样**：按时间轴每 N 秒抽一帧。默认 N=5s，长视频可自适应加大间隔。
2. **场景切换检测**：用 FFmpeg 的 `select='gt(scene,0.3)'` 检测镜头切换，只在切镜时抽帧，避免冗余。

```bash
# 每 5 秒抽一帧示例
ffmpeg -i input.mp4 -vf "fps=1/5,scale=224:224" -q:v 2 frame_%04d.jpg
```

**成本估算**：一部 2 小时视频 → 约 1440 帧（5s 间隔），经场景去重后通常可压到 200–400 帧/部。

### 3.3 视觉理解层（VLM）

**职责**：让机器"看懂"画面，生成结构化的场景描述（captions），弥补纯视觉向量对抽象概念的理解不足。

**选型**（二选一，按预算）：
| 方案 | 模型 | 适用 |
|------|------|------|
| 本地开源 | Qwen2-VL-7B / LLaVA | 数据不出内网、成本可控、需 GPU |
| 云端 API | GPT-4V / Claude / Gemini | 效果好、免运维、按量付费 |

**Prompt 模板**：
```
你是视频关键帧分析器。请用简洁中文描述这张图片中的：
1. 主要场景（地点、环境）
2. 主要物体与人物动作
3. 显著视觉特征（颜色、天气、服饰）
4. 适合检索的关键词（3-5 个）
输出为 JSON。
```

**产出**：每帧的 `{scene, objects, keywords}` 结构，存入元数据库，供**文本检索 + 向量检索双通道**使用。

> 视觉描述文本与画面向量叠加，能显著提升"会议室/海边"这类语义词的召回率。

### 3.4 多模态向量化层

**职责**：把"画面"和"文本"映射到同一语义空间，实现跨模态相似度计算。

**选型**：
| 模型 | 维度 | 说明 |
|------|------|------|
| CLIP ViT-L/14 | 768 | 经典开源双塔模型，中文适配需微调 |
| SigLIP ViT-L | 768 | Google 新版，对比学习更稳，中文更好 |
| OpenAI CLIP | 512 | 云 API，免运维 |

**核心操作**：
- **图像编码**：每帧 → 图像 embedding（768 维）
- **文本编码**：查询词 → 文本 embedding（与图像同空间）
- **同空间性质**：文本"海边"的向量 ≈ 海景帧的向量，余弦相似度高 → 这就是"画面级语义匹配"的数学基础。

**关键点**：中文场景建议用 `Chinese-CLIP` 或对 CLIP 做中文微调，否则"海边"这类中文词对齐效果差。

### 3.5 向量入库层

**职责**：存储海量向量并提供高效最近邻（ANN）检索。

**选型对比**：
| 方案 | 规模 | 部署 | 备注 |
|------|------|------|------|
| **Milvus**（推荐） | 亿级 | 自托管/云 | 功能全，内置 HNSW/IVF 索引，Python SDK 完善 |
| Qdrant | 千万级 | 自托管/云 | 轻量、Rust 实现、上手快 |
| FAISS | 百万级 | 内存库 | Meta 开源，简单但无分布式 |
| pgvector | 百万级 | PostgreSQL 插件 | 与元数据同库，运维最简单 |

**索引配置**：
```
索引类型 : HNSW  (分层可导航小世界图)
维度     : 768
距离度量 : COSINE（余弦相似度）
M        : 16
efConstruction : 128
```

**数据模型**（每条向量记录）：
```json
{
  "id": "frame_uuid",
  "video_id": "xxx",
  "timestamp": 1234,        // 帧在视频中的秒数，用于定位
  "vector": [0.1, 0.2, ...],
  "scene_desc": "海边日落，人物散步",
  "keywords": ["海边", "日落", "散步"]
}
```

### 3.6 查询与检索层

**在线查询流程**：
1. 用户输入关键词 → 用同一 CLIP 文本编码器得到查询向量。
2. 在 Milvus 做 Top-K（如 K=100）ANN 检索，得到候选帧。
3. 聚合：把同 `video_id` 的候选帧合并，统计命中帧数 → 视频级打分。
4. Rerank：用 cross-encoder（如 BGE-Reranker / MiniLM）对候选做精排。
5. 返回：视频列表 + 命中帧的**时间轴定位**（可直接跳转到对应片段）。

**混合检索策略**（提升召回）：
```
综合分 = α · 向量相似度 + β · 文本BM25命中 + γ · 视觉描述关键词命中
```
默认 α=0.6, β=0.25, γ=0.15，可调。

---

## 4. 数据流与 ER 设计

**核心数据表**（PostgreSQL）：

| 表 | 关键字段 |
|----|---------|
| `videos` | id, platform, platform_id, title, description, tags, duration, cover_url, created_at |
| `video_frames` | id, video_id(FK), timestamp, scene_desc, keywords, vector_id |
| `search_log` | id, query, query_vector, results, latency, ts |

**向量**存于 Milvus，`video_frames.vector_id` 关联 Milvus 中向量主键。

---

## 5. 技术栈汇总

| 层 | 技术选型 |
|----|---------|
| 语言 | Python 3.11+ |
| 采集 | yt-dlp, requests, 平台官方 API |
| 抽帧 | FFmpeg |
| 任务调度 | Celery + Redis |
| 视觉理解 | Qwen2-VL（本地）或 GPT-4V（云端） |
| 多模态向量 | Chinese-CLIP / SigLIP |
| 向量库 | Milvus |
| 元数据库 | PostgreSQL |
| 检索服务 | FastAPI + Milvus SDK |
| 重排 | BGE-Reranker / cross-encoder |
| 部署 | Docker Compose / Kubernetes |

---

## 6. 实施路线

**Phase 1 — 最小可用版（MVP）**（1-2 周）
- 单平台（如 YouTube/Bilibili）采集 1000 部视频
- 5s 均匀抽帧 + CLIP 向量化 + Milvus 检索
- 提供 FastAPI 查询接口 + 简单 Web 前端

**Phase 2 — 质量提升**（2-3 周）
- 引入 Qwen2-VL 场景描述，加入混合检索
- 加入 Rerank 精排，调优阈值
- 时间轴定位 + 结果片段预览

**Phase 3 — 规模化**（3-4 周）
- Celery 分布式采集，支持多平台
- 索引分片、水平扩容
- 在线增量更新（新视频自动入库）

---

## 7. 风险与应对

| 风险 | 应对 |
|------|------|
| 中文语义对齐差 | 用 Chinese-CLIP 或微调 |
| 抽帧成本高 | 场景切换检测 + 帧率自适应 |
| 向量库查询慢 | HNSW 索引 + GPU 加速 + 分片 |
| 平台 API 限流 | 指数退避重试 + 多账号轮询 |
| 长视频帧太多 | 按镜头聚类，每镜头保留代表帧 |
| 版权合规 | 仅对授权/自有内容建库，遵守平台条款 |

---

*本文档配套：架构图、匹配原理交互演示。落地时建议先跑 Phase 1 MVP 验证效果再扩展。*
