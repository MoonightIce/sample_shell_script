# 下载产物与运行数据（不推送远端）
video_search/data/
video_search/scripts/__pycache__/
**/__pycache__/
*.pyc

# 视频/分片兜底（防止误提交）
*.mp4
*.ts
*.vtt

# WorkBuddy 项目数据（记忆/会话，不推送）
.workbuddy/

# 系统文件
.DS_Store

# 项目虚拟环境(自举安装 playwright, 不推送)
video_search/.venv/

# music_search_cdp: local node deps (reinstall with npm install puppeteer-core)
music_search_cdp/node_modules/
.superpowers/
music_charts/.venv/

# music_charts: run output, not committed
music_charts/output/

# 根目录榜单抓取运行产物(日期化 JSON / 重试记录)
/data/
